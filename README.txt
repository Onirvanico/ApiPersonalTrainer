# ApiPersonalTrainer
Para executar a api, é preciso inserir as dependências necessárias para subir o servidor. Para isso,  execute o comando "npm install", na pasta raiz do projeto. 
Baixe o mongoDB e faça o set da variável na path do sistema, com isso poderá executar os comandos desse SGBD no cmd, em qualquer local.
